<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Hello, <%= name + ' ' + surname %>!</h1>
    <p>You are <%= Math.floor(Math.abs(new Date() - new Date(dob)) / (1000 * 60 * 60 * 24 * 365)) %> years old!</p>
</body>
</html>